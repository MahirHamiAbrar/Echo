{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "hot": "bun run --hot index.ts",
    "watch": "bun run --watch index.ts",
    "prod": "bun run index.ts",
    "start": "bun run prod"
  }
}
